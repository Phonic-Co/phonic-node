/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../mock-server/MockServerPool";
import { PhonicClient } from "../../src/Client";
import * as Phonic from "../../src/api/index";

describe("Auth", () => {
    test("create_session_token (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new PhonicClient({
            apiKey: "test",
            environment: { base: server.baseUrl, production: server.baseUrl },
        });
        const rawRequestBody = { ttl_seconds: 300 };
        const rawResponseBody = { session_token: "psession_abc123def456...", expires_at: "2025-07-30T23:50:00Z" };
        server
            .mockEndpoint()
            .post("/auth/session_token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.auth.createSessionToken({
            ttl_seconds: 300,
        });
        expect(response).toEqual({
            session_token: "psession_abc123def456...",
            expires_at: "2025-07-30T23:50:00Z",
        });
    });

    test("create_session_token (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new PhonicClient({
            apiKey: "test",
            environment: { base: server.baseUrl, production: server.baseUrl },
        });
        const rawRequestBody = { ttl_seconds: undefined };
        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .post("/auth/session_token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.createSessionToken({
                ttl_seconds: undefined,
            });
        }).rejects.toThrow(Phonic.BadRequestError);
    });

    test("create_session_token (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new PhonicClient({
            apiKey: "test",
            environment: { base: server.baseUrl, production: server.baseUrl },
        });
        const rawRequestBody = { ttl_seconds: undefined };
        const rawResponseBody = { error: undefined };
        server
            .mockEndpoint()
            .post("/auth/session_token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.createSessionToken({
                ttl_seconds: undefined,
            });
        }).rejects.toThrow(Phonic.UnauthorizedError);
    });

    test("create_session_token (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new PhonicClient({
            apiKey: "test",
            environment: { base: server.baseUrl, production: server.baseUrl },
        });
        const rawRequestBody = { ttl_seconds: undefined };
        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .post("/auth/session_token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.createSessionToken({
                ttl_seconds: undefined,
            });
        }).rejects.toThrow(Phonic.ForbiddenError);
    });

    test("create_session_token (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new PhonicClient({
            apiKey: "test",
            environment: { base: server.baseUrl, production: server.baseUrl },
        });
        const rawRequestBody = { ttl_seconds: undefined };
        const rawResponseBody = { error: undefined };
        server
            .mockEndpoint()
            .post("/auth/session_token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.createSessionToken({
                ttl_seconds: undefined,
            });
        }).rejects.toThrow(Phonic.InternalServerError);
    });
});
