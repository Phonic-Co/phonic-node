/**
 * This file was auto-generated by Fern from our API Definition.
 */

export interface CreateCodecRequest {
    /** The codec name. Cannot contain a slash (/). */
    name: string;
    /** Latent sample rate (must be positive). */
    latent_sample_rate: number;
    /** Waveform sample rate (must be positive). */
    waveform_sample_rate: number;
}
